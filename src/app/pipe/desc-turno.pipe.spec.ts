import { DescTurnoPipe } from './desc-turno.pipe';

describe('DescTurnoPipe', () => {
  it('create an instance', () => {
    const pipe = new DescTurnoPipe();
    expect(pipe).toBeTruthy();
  });
});
